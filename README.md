### Installation

- Clone the repository in your computer
- Run command: `npm install`
- Run command `npx lerna bootstrap`
- In the `packages/client` run `npm start`
- You should see the error in the browser's console.
